apiVersion: v1
kind: Service
metadata:
  name: {{ include "traffico-api.fullname" . }}
  labels:
    {{- include "traffico-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "traffico-api.selectorLabels" . | nindent 4 }}